# Daily Log: 2026-06-23

## 1. Today's Goal

Check whether the local Sol-RL / Sana environment is installed, then carefully finish missing dependencies if safe.

## 2. Code Changes

- Updated `memory/project_memory.md` with the actual local Sana environment status.
- Added this daily log.

## 3. Experiments Run

### Experiment ID: exp_20260623_001

- Goal: Check local Sana / Sol-RL environment status.
- Machine: local workstation.
- GPU: NVIDIA GeForce RTX 3070 Laptop GPU, 8GB VRAM.
- Driver: 580.126.09.
- CUDA: host PyTorch sees CUDA 12.8 stack; sandbox preflight may report CUDA unavailable.
- Environment: conda `sana`, Python 3.11.
- Commit hash: `rltask/Sana` at `51baa3c`.
- Config: `lmm_rollout_project/scripts/env_check/solrl_local_preflight.sh`.
- Seed: none.
- Precision mode: none.
- Task: import/preflight only.
- Number of episodes: 0.
- Log path: terminal.
- Checkpoint path: none.
- Video path: none.

Result:

- `torch 2.9.1+cu128` installed.
- Host execution sees CUDA and the RTX 3070 Laptop GPU.
- `sana`, `diffusers`, `transformers`, `xformers`, `mmcv`, and `bitsandbytes` installed.
- `flash_attn` missing.
- `transformer-engine[pytorch]` missing; this is only required for NVFP4 paths.

### Experiment ID: exp_20260623_002

- Goal: Try to install missing `flash-attn` safely with low local resource usage.
- Machine: local workstation.
- Environment: conda `sana`.
- Command policy: `nice -n 19`, `ionice -c3`, `MAX_JOBS=1`, `NVCC_THREADS=1`, `CMAKE_BUILD_PARALLEL_LEVEL=1`, no pip cache.
- Number of episodes: 0.

Result:

- Install was interrupted intentionally.
- Memory pressure rose quickly: swap reached 100% usage.
- No residual `flash-attn`, `pip install`, `nvcc`, or `ptxas` process remained after interruption.

## 4. Results

- Local Sana env is partially configured and usable for imports/code reading.
- Local Sana env is not complete for full Sol-RL training because `flash-attn` is missing.
- Local machine is not safe for compiling `flash-attn`.

## 5. Observations

- Even a single-thread, low-priority `flash-attn` install is too risky on this laptop.
- Finish `flash-attn` on the 4090/NVIDIA server or another larger machine.
- Do not install `transformer-engine[pytorch]` locally unless the NVFP4 path is explicitly needed and the machine has enough resources.

## 6. Problems

- Local swap was already near/full during dependency build.
- Continuing the build would risk another freeze/reboot.

## 7. Next Steps

1. Treat local Sana as import/preflight only.
2. Install `flash-attn` on the 4090 server after the machine is available.
3. Install `transformer-engine[pytorch]` only if running Sol-RL NVFP4/FP4 configs.

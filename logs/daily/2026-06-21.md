# Daily Log: 2026-06-21

## 1. Today's Goal

Avoid local machine freezes by stopping local heavy setup work and consolidating project control files.

## 2. Code Changes

- Moved project control files into `lmm_rollout_project/`:
  - docs, memory, skills, logs, configs, scripts, experiments, results.
- Added `lmm_rollout_project/README.md` as the entry point for project management files.
- Updated deployment and env-check scripts to resolve paths from `lmm_rollout_project/` back to the workspace root.
- Updated project memory and skill with a local resource policy.

## 3. Experiments Run

No experiments were run.

## 4. Results

- No residual `flash-attn`, `pip install`, `git-lfs`, or HSSD download process was found after the machine restart.
- Local heavy tasks are paused.

## 5. Observations

- The local laptop is too fragile for long dependency builds and large LFS pulls.
- `flash-attn` build and HSSD `git lfs pull` should be treated as remote-only or explicit-approval tasks.

## 6. Problems

- Local machine froze/rebooted during heavy setup attempts.
- Likely causes: CPU/RAM pressure from `flash-attn` build and sustained disk/network IO from HSSD LFS download.

## 7. Next Steps

1. Continue environment setup on a NVIDIA RTX server instead of the local laptop.
2. Use local machine only for code edits, docs, light imports, and short checks.
3. Finish OVMM data and rendering smoke remotely.
